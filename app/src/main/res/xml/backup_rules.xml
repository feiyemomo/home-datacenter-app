<?xml version="1.0" encoding="utf-8"?>
<!--
   Backup rules: sensitive preferences and local databases are strictly
   excluded from backup to prevent credential leaks and keystore desync.
-->
<full-backup-content>
    <exclude domain="sharedpref" path="."/>
    <exclude domain="database" path="."/>
</full-backup-content>
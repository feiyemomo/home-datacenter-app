<?xml version="1.0" encoding="utf-8"?>
<!-- Compact single-row log entry for the Dashboard "最近日志" card.
     v1.6.38: switched to bg_card_rounded for visual consistency
     with the dashboard's other cards (weather, network, alerts).
     Fixed deprecated android:tint → app:tint. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:gravity="center_vertical"
    android:background="@drawable/bg_card_rounded"
    android:layout_marginBottom="6dp"
    android:minHeight="48dp">

    <ImageView
        android:id="@+id/ivLogIcon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_history"
        android:contentDescription="@null"
        app:tint="@color/text_secondary" />

    <TextView
        android:id="@+id/tvLogMessage"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="8dp"
        android:textSize="13sp"
        android:textColor="@color/text_primary"
        android:maxLines="2"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/tvLogTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="@color/text_hint"
        android:maxLines="1"
        android:ellipsize="end" />

</LinearLayout>

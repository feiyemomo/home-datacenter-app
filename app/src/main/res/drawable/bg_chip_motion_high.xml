<?xml version="1.0" encoding="utf-8"?>
<!-- v1.6.3: bright-orange pill for high-motion-score chips (top 25%).
     v1.6.8: merged with mid-tier — both MID and HIGH chips now use
     the same amber/yellow color per user request "黄，橙色chip统一
     合并为黄色". The internal tier classification (MID/HIGH) is kept
     for backend logic (chip merging, sort order), but visually they
     render as a single yellow tier so the user sees three color
     buckets instead of four: teal (low) / yellow (mid+high) / red
     (AI alert). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFB300" />
    <corners android:radius="14dp" />
</shape>

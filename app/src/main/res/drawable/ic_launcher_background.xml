<?xml version="1.0" encoding="utf-8"?>
<!-- Solid navy background for the adaptive icon. The foreground
     already carries its own gradient, so this just provides a
     matching base color in case the launcher masks the foreground. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#FF0F172A"
        android:pathData="M0,0h108v108h-108z" />
</vector>

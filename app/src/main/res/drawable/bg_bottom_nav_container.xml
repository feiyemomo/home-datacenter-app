<?xml version="1.0" encoding="utf-8"?>
<!-- Floating capsule background for the bottom navigation bar.
     Used by activity_main.xml — wraps BottomNavigationView in a
     rounded, shadowed, translucent container that floats above the
     content (iOS 18 / Material Expressive style). -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Drop shadow (drawn under the card) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#0A000000" />
            <corners android:radius="28dp" />
        </shape>
    </item>
    <!-- Main card body -->
    <item android:top="1dp" android:bottom="1dp" android:left="1dp" android:right="1dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/glass_bg" />
            <corners android:radius="28dp" />
            <stroke
                android:width="1dp"
                android:color="@color/glass_border" />
        </shape>
    </item>
</layer-list>
